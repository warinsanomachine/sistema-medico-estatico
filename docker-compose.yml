version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: sistema-medico-web
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html:ro
    restart: unless-stopped

